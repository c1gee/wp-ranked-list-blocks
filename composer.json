{
	"name": "c1gee/wp-ranked-list-blocks",
	"description": "Semantically marked-up ranked list blocks for WordPress with ItemList schema for search and AI ingestion",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"version": "1.1.0",
	"require": {
		"php": ">=8.1"
	}
}
